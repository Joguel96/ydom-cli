{
  "name": "{{PROJECT_NAME}}",
  "version": "0.0.1",
  "description": "A YDOM application",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "ydom-core": "^2.0.1"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vite": "^8.0.2"
  }
}
